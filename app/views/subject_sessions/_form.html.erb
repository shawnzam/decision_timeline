<%= form_for(@subject_session) do |f| %>
  <% if @subject_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subject_session.errors.count, "error") %> prohibited this subject_session from being saved:</h2>

      <ul>
      <% @subject_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :timeline_id %><br>
    <%= f.text_field :timeline_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
